const geminiKey = 'GEMINI_API_KEY';
const supabaseUrl = 'SUPABASE_URL';
const supabaseAnonKey = 'SUPABASE_ANON_KEY';
const storageBucket = 'SUPABASE_STORAGE_BUCKET';

export default { geminiKey, supabaseAnonKey, supabaseUrl, storageBucket };
